## Packages
framer-motion | Page transitions and detailed animations
clsx | Utility for constructing className strings
tailwind-merge | Utility to merge Tailwind classes
lucide-react | Icons for navigation and UI elements
uuid | For generating local IDs

## Notes
Using LocalStorage for data persistence as requested.
Context API used for global state management (Meals, Recipes, Grocery, Language).
No backend API calls are made from the frontend; it is fully client-side.
Shadcn UI components are expected to be present in @/components/ui based on the file list.
